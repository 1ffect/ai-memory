# 开源记忆机制案例研究
核对日期：2026-09-18。以公开资料、代码与机制为研究对象，不限定助手或 Agent；暂不展开产品落地和验收。未运行这些项目，示意流程不是测评结果。
## 品牌与项目的对应边界
- 千问：QwenLM/Qwen-Agent 官方文件 Memory 模块。
- Kimi：MoonshotAI/kimi-cli 官方压缩与会话恢复实现。
- 豆包相关：volcengine/OpenViking 是火山引擎开源项目；不能据此断言它就是豆包 App 的内部实现。

## Qwen-Agent

QwenLM / 千问团队官方仓库 · 文件检索型 Memory

**记什么：** 系统文件与会话中上传的文件内容；这里的 Memory 主要服务于资料检索。

**怎么写：** 收集并去重支持格式的文件，交给解析与检索工具；不是自动从所有聊天提取个人偏好。

**怎么读：** 取最后一条用户问题，可经关键词生成策略改写查询，再调用 retrieval 返回相关资料。

**怎么更新：** 当前模块重新汇集系统与会话文件；没有在这段代码中看到通用的个人偏好冲突合并流程。

**可借鉴什么：** 说明“Memory”不一定是用户画像，也可以是按问题调取资料、控制引用量的模块。

**理解边界：** 这是官方 Qwen-Agent 的公开模块，不足以证明千问消费端所有跨会话记忆都按此实现。

**证据范围：** 已读取核心源码；memory.py 文件 SHA：5a30992d98e494ce9d09a21e072c51f5e2e91d62（文件内容标识，不是仓库提交号）。

### 流程解析

1. **收集文件**：get_rag_files 合并系统文件与会话附件，筛选格式并去重。

2. **提取当前问题**：仅当最后一条是用户消息时提取问题。

3. **生成检索词**：配置了模型和策略时生成检索词；否则使用原查询。

4. **检索资料**：retrieval 接收 query 与 files，并受 max_ref_token 等配置约束。

5. **返回引用内容**：输出名为 memory 的消息，供上层使用；这是资料返回，不是永久学会了资料。

### 来源

- [源码：Memory._run / get_rag_files](https://github.com/QwenLM/Qwen-Agent/blob/main/qwen_agent/memory/memory.py)

- [官方项目说明](https://github.com/QwenLM/Qwen-Agent)

## Kimi Code CLI

MoonshotAI / 月之暗面官方仓库 · 上下文压缩 + 会话文件恢复

**记什么：** 当前任务的消息历史、压缩摘要、用量与检查点。

**怎么写：** Context.append_message 将消息追加到文件；压缩器将较早内容整理成摘要并保留近期消息。

**怎么读：** Context.restore 从会话文件恢复；后续模型调用使用整理后的消息序列。

**怎么更新：** 达到上下文比例阈值或预留空间阈值时可触发压缩；源码还提供检查点回退与清空。

**可借鉴什么：** 展示“长任务不断线”怎样由压缩和会话状态保存实现，不需要先建用户画像库。

**理解边界：** 会话恢复不等于所有新会话自动共享个人记忆；Kimi Code CLI 源码不代表 Kimi 网页助手全部内部实现。

**证据范围：** 已读取 compaction.py 与 context.py；分析范围为公开的上下文管理实现，未运行产品。

### 流程解析

1. **追加消息**：消息以逐条 JSON 记录追加到会话文件。

2. **检查上下文预算**：should_auto_compact 检查比例阈值或 token 数加预留空间是否触及窗口。

3. **保留近期消息**：SimpleCompaction 将近期部分与待压缩内容分开。

4. **压缩较早历史**：调用模型生成摘要，可接受自定义压缩侧重点。

5. **摘要接续 / 恢复**：摘要与保留消息组成新上下文；Context 支持从文件恢复任务历史。

### 来源

- [源码：压缩与触发条件](https://github.com/MoonshotAI/kimi-cli/blob/main/src/kimi_cli/soul/compaction.py)

- [源码：会话保存与恢复](https://github.com/MoonshotAI/kimi-cli/blob/main/src/kimi_cli/soul/context.py)

- [官方项目](https://github.com/MoonshotAI/kimi-cli)

## OpenViking

volcengine / 火山引擎官方仓库 · 目录化上下文 + 分层读取

**记什么：** 资源、用户记忆与技能，统一按目录和 URI 组织。

**怎么写：** 提交会话后归档对话并启动后台提取；候选记忆与已有记录比较，决定创建、合并或跳过。

**怎么读：** 先读 L0 简短摘要和 L1 概览判断相关性，再按需读取 L2 详情；检索利用目录结构。

**怎么更新：** 会话持续提交，新信息逐步并入已有记忆，避免每次加载全部原始内容。

**可借鉴什么：** 把“怎么组织”和“加载到哪一层”纳入记忆设计，兼顾可浏览性与上下文预算。

**理解边界：** 这是火山引擎相关开源实现。支持或使用豆包模型，不等于豆包 App 的内部记忆系统已开源。

**证据范围：** 已核对官方 README 与机制说明；未对完整写入调用链逐行审计。

### 流程解析

1. **提交会话**：会话提交是资料中明确的记忆生成触发点。

2. **后台提取**：从已归档会话提取候选信息，由记忆策略控制保留内容。

3. **比较已有记忆**：与已有记录比较，而不是把所有片段无条件加入。

4. **创建 / 合并 / 跳过**：将处理结果归入目录化的上下文体系。

5. **分层读取**：L0 用于快速判断，L1 用于了解结构，L2 按需读取原始详情。

### 来源

- [官方机制与目录示意](https://github.com/volcengine/OpenViking)

- [配置与检索配置边界](https://github.com/volcengine/OpenViking/blob/main/docs/en/guides/01-configuration.md)

## ReMe

agentscope-ai / AgentScope 项目仓库 · 日常记录 → 长期知识的持续整理

**记什么：** 原始会话、每日记录，以及长期偏好、经验和知识；持久内容采用 Markdown。

**怎么写：** auto_memory 从对话生成 daily 记录；auto_dream 从变化的 daily 文件中提取可复用单元。

**怎么读：** 默认可用 BM25 与文件链接查询；可选向量检索后融合，并返回可继续读取的片段。

**怎么更新：** 长期整合区分 CREATE、CORROBORATE、REFINE、CORRECT：创建、补充证据、细化、纠错。

**可借鉴什么：** 直观看到普通文件如何形成长期记忆，以及“补充细节”和“改正旧结论”如何区分。

**理解边界：** 这里分析当前 ReMe 官方开源流程，不把它等同于千问 App 的内部实现。

**证据范围：** 已读取官方生命周期与 Auto Dream 详细流程；不是自称的通用自动学习效果测评。

### 流程解析

1. **捕获会话**：保留经过过滤的会话来源，供后续追溯。

2. **生成每日记录**：将日常事实与摘要放入 daily。

3. **提取复用单元**：auto_dream 针对变化的输入提炼值得长期保留的单元。

4. **检索已有节点**：通过搜索和读取找到相关 digest 节点。

5. **整合长期记忆**：创建、补充来源、细化或纠正节点；成功处理的输入更新检查记录。

### 来源

- [官方生命周期与检索说明](https://github.com/agentscope-ai/ReMe)

- [Auto Dream：阶段与更新动作](https://github.com/agentscope-ai/ReMe/blob/main/docs/en/auto_dream.md)

## MemoryOS

BAI-LAB / 研究团队仓库 · 短期、中期、长期记忆分层

**记什么：** 近期交互、中期整理结果、长期用户画像与知识。

**怎么写：** 通过 add_memory 接收交互，按分层更新机制整理和提升信息。

**怎么读：** 通过检索模块组合不同记忆层的信息，再用于生成回答。

**怎么更新：** 公开设计包括容量与热度等配置，调节短期到中期、长期的转移。

**可借鉴什么：** 用于研究“最近说过”与“值得长期留下”如何分层，而不是全部堆在同一个列表里。

**理解边界：** MemoryOS（BAI-LAB）与 MemOS（MemTensor）是不同项目；支持 Qwen 模型不代表千问官方记忆实现。

**证据范围：** 已核对官方 README 的分层设计与模块说明；未逐行追踪更新器。

### 流程解析

1. **接收交互**：add_memory 接收用户与助手交互。

2. **短期保留**：近期交互进入短期层。

3. **中期整合**：由中期层组织和整合短期信息。

4. **更新长期画像**：根据更新策略提炼长期画像或知识。

5. **跨层检索**：检索器从不同层获取任务所需信息。具体阈值取决于版本与配置。

### 来源

- [官方机制、模块说明与示例](https://github.com/BAI-LAB/MemoryOS)

- [研究论文](https://arxiv.org/abs/2506.06326)

## MemOS

MemTensor / 开源项目仓库 · 统一记忆接口与可组合记忆集合

**记什么：** 事实、画像、工具轨迹等记忆；组织为可组合的 Memory Cube。

**怎么写：** 通过统一接口添加记忆，公开方案包含异步摄取与调度。

**怎么读：** 查询时指定可读的记忆集合，取回适用信息。

**怎么更新：** 提供编辑、删除及自然语言反馈纠正的机制；不同部署入口应分开研究。

**可借鉴什么：** 用于理解“同一套接口管理多种记忆”和“按集合选择上下文”的方案。

**理解边界：** 自托管服务、本地插件与云服务不能视为同一实现；本页不据营销性能数字做选型。

**证据范围：** 已核对官方 README 的机制与自托管接口示例；未追踪各部署版本的完整内部逻辑。

### 流程解析

1. **组织记忆集合**：使用 Memory Cube 表达不同的记忆集合。

2. **接收新信息**：添加消息或其他公开接口支持的内容。

3. **处理并保存**：依据具体实现处理并持久保存。

4. **选择集合查询**：根据查询和可读集合检索相关内容。

5. **反馈修正**：反馈用于补充、纠正或替换已有信息；云端能力需与开源范围区分。

### 来源

- [官方架构与接口示例](https://github.com/MemTensor/MemOS)

## MemGPT / Letta

letta-ai / 项目官方仓库 · Agent 主动调用记忆工具

**记什么：** 核心信息、历史交流与外部档案，经典设计采用分层组织。

**怎么写：** 模型判断何时需要新增或编辑记忆，再调用工具由程序执行。

**怎么读：** 任务中缺少历史依据时，Agent 主动搜索或读取外部记忆。

**怎么更新：** 执行过程中持续维护状态；经典架构与当前 Letta 代码需分开阅读。

**可借鉴什么：** 展示记忆管理如何成为 Agent 自身的动作选择，而非全部由固定后台流程决定。

**理解边界：** 本页描述 MemGPT 经典模式；当前 Letta 活跃源码入口与旧 V1 归档不同。

**证据范围：** 依据经典论文与官方版本说明；不把经典 API 当作当前接口承诺。

### 流程解析

1. **识别任务**：模型接收任务与可见上下文。

2. **判断记忆需求**：判断是否需要历史资料或需要保存新信息。

3. **调用读写工具**：选择记忆搜索、写入或编辑工具。

4. **程序执行**：运行程序执行操作并返回结果。

5. **继续任务**：带着新信息继续任务，后续也可能再次操作记忆。

### 来源

- [经典论文](https://arxiv.org/abs/2310.08560)

- [官方仓库与历史入口](https://github.com/letta-ai/letta)

- [当前代码入口](https://github.com/letta-ai/letta-code)

## Mem0

mem0ai / 项目官方仓库 · 应用读写之间插入记忆层

**记什么：** 从交流中提取的可复用事实、偏好及任务信息。

**怎么写：** 应用调用 add，选择直接保存或进行记忆提取，并传入用户和元数据。

**怎么读：** 应用调用 search，将相关记忆加入模型输入，再生成回答。

**怎么更新：** 应按版本研究；当前官方描述包含 ADD-only 追加路线，由时间与检索信号处理新旧内容。

**可借鉴什么：** 适合理解“在已有应用旁边增加记忆模块”的最小调用链。

**理解边界：** 开源 SDK 与托管平台的优化不同；不能把旧版覆盖更新规则概括成所有当前实现。

**证据范围：** 依据此前读取的官方示例与公开代码片段；托管服务内部优化不视为已公开实现。

### 流程解析

1. **收到问题**：应用识别当前用户和请求。

2. **search 查询**：在相应范围内查询相关记忆。

3. **组装上下文**：把查询结果和当前要求一起交给模型。

4. **生成回答**：模型生成回答。

5. **add 新信息**：应用按策略提交新信息；调用示例每轮保存，不意味着所有应用都必须如此。

### 来源

- [官方示例与版本说明](https://github.com/mem0ai/mem0)

- [源码入口：add 与记忆处理](https://github.com/mem0ai/mem0/blob/main/mem0/memory/main.py)

## Graphiti

getzep / 项目官方仓库 · 带时间的关系与历史记忆

**记什么：** 实体、事实关系及其来源事件。

**怎么写：** 将输入事件提取为实体与关系，并处理重复或应失效的旧关系。

**怎么读：** 结合语义、关键词、图关系及时间条件查询。

**怎么更新：** 事实变化时可标记旧关系失效，保留历史，而不是只覆盖成新值。

**可借鉴什么：** 解释“当前谁负责”和“过去谁负责”如何同时回答。

**理解边界：** 时间结构不能保证自然语言推断正确；Graphiti 开源框架与 Zep 托管服务需区分。

**证据范围：** 依据官方机制说明及此前读取的实体、关系处理代码片段。

### 流程解析

1. **接收事件**：将消息或资料作为来源事件。

2. **识别实体**：识别人、项目等实体，并对已有实体进行归并。

3. **提取关系**：提取实体之间的关系。

4. **处理有效期**：对确实被替代的事实记录结束时间，保留历史依据。

5. **查询当前 / 历史**：按问题的时间范围使用当前或过去关系。

### 来源

- [官方时间图机制](https://github.com/getzep/graphiti)

- [源码入口：实体与关系处理](https://github.com/getzep/graphiti/blob/main/graphiti_core/graphiti.py)

## LangMem

langchain-ai / 项目官方仓库 · 旧记忆与新对话的整合

**记什么：** 固定字段的用户档案，或独立保存的事实与经历集合。

**怎么写：** 输入对话和旧记忆，让模型输出整合结果，再由存储层持久化。

**怎么读：** 按键、语义与元数据等方式读取；支持 Agent 记忆工具。

**怎么更新：** 支持实时更新、后台整理，以及依据交互反馈生成提示词改进方案。

**可借鉴什么：** 把“生成新状态”和“实际保存”分开，也展示档案与记忆集合的区别。

**理解边界：** 提示词改进只是候选行为变化，不能自动证明效果提升；进程内存不等于持久存储。

**证据范围：** 依据官方概念说明与公开调用示例；未对完整库逐行审计。

### 流程解析

1. **新对话 + 旧记忆**：同时提供新信息与已有记忆。

2. **模型整合**：判断哪些内容需要新增、补充或修改。

3. **生成新状态**：核心函数返回整理后的状态。

4. **存储管理器**：高层存储集成执行持久写入。

5. **后续调用**：后续任务按需读取，实时与后台时机可分别设计。

### 来源

- [官方概念与工作流](https://langchain-ai.github.io/langmem/concepts/conceptual_guide/)

- [源码仓库](https://github.com/langchain-ai/langmem)

## Cognee

topoteretes / 开源项目仓库 · 知识图谱 + 向量 + 关系元数据

**机制要点：** 通过 `remember` 摄取数据，以图谱、向量和关系元数据组织知识；`recall` 根据查询路由检索；`improve` 与 `forget` 分别承担后续整理和删除。会话可按 `session_id` 隔离，并提供会话蒸馏路径。

**可借鉴什么：** 把记忆看成可组合的数据管道，而不只是一个向量集合；召回可以根据问题在多种结构之间选择。

**理解边界：** 官方仓库公开的是框架能力；具体图模型、嵌入模型、图数据库和部署配置会改变结果。

**来源：** [Cognee 官方仓库](https://github.com/topoteretes/cognee)

## A-MEM

AGI Research / 论文与开源实现 · Zettelkasten 式自组织记忆网络

**机制要点：** 每条记忆不只保存文本，还生成关键词、标签和上下文描述；写入时根据语义邻居建立链接，并允许新记忆促使旧邻居演化。公开实现以 ChromaDB 承载向量检索。

**可借鉴什么：** 写入不是简单追加，而是一次局部知识网络重组；适合研究“新经验如何改变旧记忆的组织方式”。

**理解边界：** 论文原型与完整生产系统不同；自动演化也可能放大抽取错误，需要保留来源和审计路径。

**来源：** [A-MEM 官方仓库](https://github.com/agiresearch/A-mem) · [论文](https://arxiv.org/abs/2502.12110)

## Memobase

memodb-io / 开源项目仓库 · 用户画像 + 事件时间线

**机制要点：** 原始聊天先作为 Blob 进入缓冲区，再按 Token 阈值、空闲时间或手动 flush 触发处理，生成用户 Profile 与事件 Timeline；应用按预算获取可直接注入的用户上下文。

**可借鉴什么：** 将“低延迟读取稳定画像”和“后台批量处理对话”分开；适合面向大量用户的画像型记忆。

**理解边界：** 默认流程可在处理后删除原始 Blob；若需要审计或重新提取，应先设计单独的原始证据保留策略。

**来源：** [Memobase 官方仓库](https://github.com/memodb-io/memobase)

## Generative Agents

Stanford / Google Research 论文配套仓库 · 记忆流 + 反思 + 计划

**机制要点：** 观察持续进入 Memory Stream；读取时综合新近度、重要性和相关性；累积到一定程度后生成更高层反思，再参与后续计划与行为。

**可借鉴什么：** 说明长期行为连续性不仅依赖检索，还依赖从零散经历中形成更高层解释。

**理解边界：** 这是研究模拟环境，检索公式和行为效果不能直接外推到真实用户记忆产品。

**来源：** [Generative Agents 官方仓库](https://github.com/joonspk-research/generative_agents) · [论文](https://arxiv.org/abs/2304.03442)

## LazyMem

匿名研究仓库 · 保留原始历史，查询时选择性构造

**机制要点：** 不在写入时过早把所有历史压成固定事实；查询出现后，用稠密检索、BM25、RRF 与交叉编码器扩大候选，再恢复局部上下文，通过 KEEP / DROP 和查询相关压缩形成最终证据。

**可借鉴什么：** 当未来问题未知时，延迟有损压缩可能比“预先猜测什么重要”更稳健。

**理解边界：** 当前为匿名双盲研究发布，仓库未完整提供权重、数据、检查点与许可证；适合作为研究路线，不应视为成熟生产组件。

**来源：** [LazyMem 研究仓库](https://github.com/allacnobug/LazyMem) · [论文](https://arxiv.org/abs/2607.22690)

## 评测入口

- [LoCoMo](https://github.com/snap-research/locomo)：长对话中的单跳、多跳、时间与开放域问题。
- [LongMemEval](https://github.com/xiaowu0162/LongMemEval)：信息提取、多会话推理、知识更新、时间推理与拒答。
- [MemoryAgentBench](https://arxiv.org/abs/2507.05257)：准确检索、测试时学习、长程理解与选择性遗忘。
- [LongMemEval-V2](https://github.com/xiaowu0162/LongMemEval-V2)：真实 Agent 轨迹中的状态、流程经验、环境坑点与前提意识。

横向比较时必须固定数据版本、骨干模型、检索与 Token 预算、Judge 和 Prompt，并同时记录写入延迟、查询延迟、成本与删除残留。公开排行榜本身不足以解释一个系统为什么成功或失败。
